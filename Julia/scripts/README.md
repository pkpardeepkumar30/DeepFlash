# Pipelines

Source code for pipe flow simulations.

## Environment setup

From this(pipelines) folder, run `julia setup.jl` to set up the environment.

In VSCode, click "Julia env" at the bottom bar, and select the "Pipe" folder.
Then the scripts can be run.